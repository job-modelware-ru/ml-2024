export interface ProfileResponse {
  first_name: string;
  last_name: string;
  username: string;
  email: string;
  is_verified: boolean;
  is_staff: boolean;
  is_active: boolean;
  date_joined: string;
}
